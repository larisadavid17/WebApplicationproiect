@page "{id:int?}"
@model WebApplicationproiect.Pages.Specializari.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Specializare[0].DenumireSpecializare)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.SpecializareData.Specializari)
 {
 string selectedRow = "";
 if (item.ID == Model.SpecializareID)
 {
 selectedRow = "table-success";
 }
 <tr class="@selectedRow">

            <td>
                @Html.DisplayFor(modelItem => item.DenumireSpecializare)
            </td>
            <td>
                    <a asp-page="./Index" asp-route-id="@item.ID">Select</a> |
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
@if (Model.SpecializareData.Angajati != null)
{
    <h3>Angajati care au aceasta Specializare</h3>
    <table class="table">

        @foreach (var item in Model.SpecializareData.Specializari)
        {
            string selectedRow = "";
            if (item.ID == Model.SpecializareID)
            {
                selectedRow = "table-success";
            }
            <tr class="@selectedRow">

                <td>
                   
                </td>
            </tr>
        }
    </table>
}
